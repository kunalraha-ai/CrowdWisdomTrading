[pytest]
# Register markers to prevent warnings
markers =
    integration: mark test as hitting real external networks/APIs (skip by default)

# Configure default options to run only offline/mocked unit tests
addopts = -m "not integration"
